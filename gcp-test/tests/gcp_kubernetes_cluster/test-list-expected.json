[
  {
    "akas": [
      "{{ output.resource_aka.value }}"
    ],
    "location": "{{ output.location.value }}",
    "name": "{{ resourceName }}",
    "services_ipv4_cidr": "{{ output.services_ipv4_cidr.value }}"
  }
]