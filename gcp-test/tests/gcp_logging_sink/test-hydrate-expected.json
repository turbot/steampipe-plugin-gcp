[
  {
    "description": "Test sink to verify the table",
    "destination": "pubsub.googleapis.com/projects/{{ output.project_id.value }}/topics/{{ resourceName }}",
    "disabled": false,
    "filter": "resource.type = gce_route",
    "include_children": false,
    "name": "{{ resourceName }}"
  }
]
